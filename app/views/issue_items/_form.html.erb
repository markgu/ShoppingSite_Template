<%= form_for(@issue_item) do |f| %>
  <% if @issue_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issue_item.errors.count, "error") %> prohibited this issue_item from being saved:</h2>

      <ul>
      <% @issue_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
  </div>
  <div class="field">
    <%= f.label :startDate %><br>
    <%= f.date_select :startDate %>
  </div>
  <div class="field">
    <%= f.label :finishDate %><br>
    <%= f.date_select :finishDate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
